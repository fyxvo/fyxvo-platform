FROM rust:1.88-bookworm

RUN apt-get update \
  && apt-get install -y --no-install-recommends ca-certificates curl git docker.io pkg-config libssl-dev \
  && rm -rf /var/lib/apt/lists/*

RUN cargo install solana-verify --version 0.4.11 --locked

WORKDIR /workspace
ENTRYPOINT ["solana-verify"]
